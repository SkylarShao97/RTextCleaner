# language: r
# cache: packages

# r:
#   - release
#   - oldrel

# script:
#   - R CMD build .
#   - R CMD check *tar.gz --as-cran
language: r
r:
  - release  

cache: packages  

before_install:
  - sudo apt-get update  
  - sudo apt-get remove --purge google-chrome-stable -y || true 
  - Rscript -e 'install.packages(c("cpp11", "waldo", "gtable", "purrr"), repos="https://cloud.r-project.org")'
  - sudo rm -f /etc/apt/sources.list.d/google-chrome.list
  - sudo rm -f /etc/apt/trusted.gpg.d/google-chrome.gpg
  - sudo apt-get update



install:
  - Rscript -e 'install.packages("remotes", repos="https://cloud.r-project.org")'
  - Rscript -e 'remotes::install_deps(dependencies = TRUE)'

script:
  - R CMD build .
  - R CMD check *tar.gz --no-manual --as-cran

